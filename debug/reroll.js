const Buster = require('../models/Buster');

const rerollUsers = ['slider8'];

const reroll = async () => {
  // map reroll busters
  // query buster reroll busters
  // get all reroll dates
  // delete busters
  // query all busters
  // map dates and select random for each date setTimeout
  // sort mapped date into random busters list of dates
  // update buster
};

const main = () => {};

main();
